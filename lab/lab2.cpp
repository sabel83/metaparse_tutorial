// Copyright Abel Sinkovics (abel@sinkovics.hu)  2013.
// Distributed under the Boost Software License, Version 1.0.
//    (See accompanying file LICENSE_1_0.txt or copy at
//          http://www.boost.org/LICENSE_1_0.txt)

#include <boost/xpressive/xpressive.hpp>

#include <lab2.hpp>

#include <regex_test.hpp>

int main()
{
  return
    (
         true
//      && all_tests(regex_, "")
//      && all_tests(regex_a, "a")
//      && all_tests(regex_abc, "abc")
//      && all_tests(regex_b_, "b*")
//      && all_tests(regex_ab_, "ab*")
//      && all_tests(regex_b_c, "b*c")
//      && all_tests(regex_ab_c, "ab*c")
//      && all_tests(regex_a__c, "a.*c")
//      && all_tests(regex_a1_c, "a1*c")
//      && all_tests(regex__abc__, "(abc)*")

    ) ? 0 : 1;
}


